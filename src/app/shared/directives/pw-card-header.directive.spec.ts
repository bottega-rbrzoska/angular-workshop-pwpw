import { PwCardHeaderDirective } from './pw-card-header.directive';

describe('PwCardHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PwCardHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
